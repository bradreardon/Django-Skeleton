hosts = (
    # {
    #     'user': 'stanley',              # system username on the target host.
    #                                     # Defaults to your local username.
    #     'hostname': '123.123.123.123',  # a hostname
    #     'port': 666,                    # ssh port (you can leave this out for 22)
    #     'key_filename': '',             # path to a private key, if necessary
    #     'directory': '/home/app/app/',   # path to directory on remote server, required
    #     'binprefix': '/home/app/venv/',  # if using a virtualenv, specifiy the path to its 'bin' directory, or leave blank. trailing slash REQUIRED if using this setting.
    # },
)
